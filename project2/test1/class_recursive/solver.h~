#pragma once
#include <stdio.h>
#include <vector>
#include "umfpack.h"
#include "create_matrix.h"
using namespace std;
struct xy_solve{
	vector<double>x_cor;
	vector<double>y_cor;
};
class solver
{
	create_matrix cm;
public:
	solver();
	~solver();
	vector<double>solve_matrix();
};

